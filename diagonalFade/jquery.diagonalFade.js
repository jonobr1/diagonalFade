$.fn.diagonalFade = function() {
    var options = $.extend({},$.fn.count.defaultOptions, customOptions);
    return this.each.(function() {
        var $this = $(this);
    });
};

$.fn.diagonalFade.defaultOptions = {
    startCount: 100
};